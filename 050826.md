# $upgrade

> $upgrade

<details><summary>3 previous messages</summary>

> I’m using the `upgrade` skill now, so I’ll read its orchestrator instructions first and then follow the pipeline gates exactly.
>
> The orchestrator requires a startup preflight before any analysis: resolve the target repo, create a run artifact directory, and verify GitNexus indexing. I’m checking those inputs now.
>
> `PATH_TO_REPO` resolves to `C:\Users\OEM\legacy-upgrade-system\projects\tv-radio`, which is inside the writable workspace. I’m creating the run artifact folder and checking whether GitNexus already knows this repo.
</details>

GitNexus is available and `tv-radio` is indexed.

Artifact directory for this run:
`C:\Users\OEM\legacy-upgrade-system\.codex\upgrade-runs\tv-radio\20260805-153534-startup`

Before Stage 1, I need the required startup inputs:

1. What upgrade do you want to perform? Include target versions and any paths/modules to exclude.

2. Does this app depend on external services not fully declared in package manifests, such as Solr, MongoDB, Memcached, RabbitMQ, etc.? Include current and target versions if known, or say unsure.

3. How should this application be compiled, started, or smoke-tested after the upgrade? Please provide exact commands and working directories for important modules, or say unsure.

4. Does the target database/search index currently contain data, or is it empty? You can answer `has data`, `empty`, or `unknown`.

> Upgrade any outdated dependencies found in the tv-radio repo (across all modules: tv-radio-admin/Admin/API, tv-radio-admin/Common, tv-radio-frontend, and any others), including compatibility for Solr — the search server referenced under tv-radio-admin/Settings/Solr (schema.xml, solrconfig.xml, solrconfig-records.xml, currently pinned to luceneMatchVersion 4.9). Don't limit this to headline frameworks — check every dependency in every package.json for a major-version gap, including smaller libraries like logging/metrics clients (e.g. winston, winston-logstash, influx), and flag every file that calls into a flagged dependency, not just the ones with an obviously named breaking change.
>
> Also cover the vendored front-end libraries under tv-radio-admin/Admin/Interface/libs/, which are loaded via <script> tags in index.html and have no package.json entry at all: ember.prod-v1.13.3.js / ember.debug-v1.13.3.js / ember-template-compiler-v1.13.3.js (Ember 1.13.3), handlebars-v3.0.3.js, jquery-v1.11.0.js, and socket.io-1.1.0.js. There is no ember-cli build pipeline here — it's plain global scripts — so do not attempt a full Ember rewrite to the latest version; target the newest version achievable without adopting ember-cli, and explicitly flag in the plan if a real ember-cli migration would be required to go further. The vendored socket.io-1.1.0.js client must be upgraded in lockstep with whatever version tv-radio-admin/Admin/API's socket.io package is upgraded to, since a client/server Socket.IO major-version mismatch breaks the realtime connection outright. Also check tv-radio-admin/Utilities/ember-precompile.js and ember-precompile.sh, which compile the .hbs templates and depend on the deprecated optimist package.
>
> External services not in a package manifest: Solr — current version unknown beyond luceneMatchVersion 4.9 in solrconfig.xml (roughly Solr 4.x era). Target the latest version compatible with this codebase's Solr client usage; infer what you can from config/client code (Common/solr-helpers.js, Search/search-index-solr.js, tv-radio-frontend/solr-search.js) and flag anything you can't determine.
>
> To compile the admin folder, go to tv-radio-admin/Admin/API and run node app.js, then verify the website at http://localhost:8080 works.
> To compile the frontend folder, go to tv-radio-frontend and run node app.js, then verify the website at http://localhost:8082 works.
> After both startup checks pass, also run these CLI index-maintenance commands from tv-radio-admin/CLI (in this order, each with --verbose) and treat any failure the same as a startup/test failure:
> node chapman.js resetindex --verbose
> node chapman.js index --verbose
> node chapman.js geodata --verbose
> node chapman.js courses --verbose
>
> The database has no data.

<details><summary>5 previous messages</summary>

> I’ve recorded the upgrade scope, service notes, validation commands, health URLs, and the empty datastore constraint. Next I’m loading the Stage 1 analysis instructions and the multi-agent tool so the repository analysis is produced as the pipeline expects.
>
> Stage 1 will be delegated to the analysis sub-agent per the upgrade pipeline. I’m passing it the full dependency-upgrade scope, including vendored browser libraries, Solr compatibility, validation commands, and the empty datastore note so those are reflected in the impact report.
>
> The analysis agent is running. While it works, I’m doing only orchestration-side checks, mainly confirming the artifact location is ready and leaving the code analysis to the delegated stage.
>
> The analysis is taking longer than five minutes, which is plausible for this dependency-wide scan because it has to enumerate every manifest dependency and every direct usage site. I’m waiting for the Stage 1 handoff rather than starting planning early, since the plan gate depends on the impact report.
>
> Stage 1 has returned a handoff. I’m doing the required lightweight gate check now: confirming the report file exists and the required summary fields are present before I ask for your approval to plan.
</details>

**Stage 1 Complete**

Impact report: `C:\Users\OEM\legacy-upgrade-system\.codex\upgrade-runs\tv-radio\20260805-153534-startup\impact-report.json`

Summary:
- Total affected files: `44`
- High-risk files: `29`
- Breaking-change groups: `8`
- Covered all `package.json` manifests, vendored admin frontend globals, Socket.IO client/server lockstep risk, Solr 4.9-era config/client usage, and deprecated `optimist` utility usage.

Coverage notes:
- GitNexus was available and current.
- No dependency lockfiles were found, so exact installed transitive versions are unavailable.
- Solr target/runtime version remains unknown beyond `luceneMatchVersion 4.9`.
- MongoDB, Memcached, InfluxDB, and Logstash runtime versions were not discoverable from repo files.
- Vendored/minified library internals were not exhaustively enumerated, but load points, versioned files, and app-level usage were recorded.

Do you approve this analysis and want to proceed to planning?

> yes

<details><summary>4 previous messages</summary>

> Analysis is approved. I’m moving to Stage 2 and loading the planning instructions; this stage will turn the impact report into a concrete change plan and validation command list.
>
> Planning is being delegated as required. I’m including the approved analysis handoff plus your exact startup, browser, CLI, and datastore constraints so the final validator can run the same checks later.
>
> The planner is running. I’m waiting for `change-plan.json`; once it returns, I’ll verify the artifact exists and summarize the planned changes for your approval before any repo branch or code changes happen.
>
> Stage 2 has returned. I’m checking that `change-plan.json` exists and contains the required planner fields before presenting the approval gate.
</details>

**Stage 2 Complete**

Change plan: `C:\Users\OEM\legacy-upgrade-system\.codex\upgrade-runs\tv-radio\20260805-153534-startup\change-plan.json`

Summary:
- Total planned changes: `49`
- Planned files: `49`
- High-risk areas:
  - Package manifests with major-version jumps
  - Socket.IO server/client lockstep upgrade
  - MongoDB driver 2.x to 7.x adapter changes
  - Solr 4.9-era schema/config/client compatibility
  - Admin Ember globals capped at Ember 1.13.x without ember-cli
  - Frontend `winston` / `winston-logstash` logger rewrite
- Bounded scope-expansion allowance: `3` files, only for runtime failures caused by already-approved dependency upgrades.

Validation planned:
- `8` npm setup commands
- `2` `node --check` compile checks
- Bounded startup probes for:
  - `http://localhost:8080/`
  - `http://localhost:8082/`
- Required CLI smoke commands:
  - `node chapman.js resetindex --verbose`
  - `node chapman.js index --verbose`
  - `node chapman.js geodata --verbose`
  - `node chapman.js courses --verbose`

Do you approve this plan and want to proceed to test generation and execution?

> yes

<details><summary>8 previous messages</summary>

> Plan is approved. I’m starting Stage 3 by creating the upgrade branch before any generated tests are written, then I’ll load the test-generation instructions.
>
> The target repo is currently dirty before my edits: `.claude/settings.local.json` deleted, `AGENTS.md` modified, and `CLAUDE.md` modified. I won’t revert those; I’ll create the upgrade branch on top of the current worktree and keep later changes scoped around them.
>
> The branch `upgrade/dependency-modernization-solr` is created. I’m reading the Stage 3 test instructions and will delegate small smoke/integration test generation next.
>
> Stage 3 is being delegated now. The test agent is constrained to create only a small smoke/integration set, update `change-plan.json`, and avoid source changes.
>
> The test generation agent is running. After it reports back, execution can proceed without another approval gate unless test generation fails.
>
> Stage 3 completed: it added two focused upgrade tests and recorded their commands in `change-plan.json`. I’m moving directly to Stage 4 execution, as the pipeline specifies no approval gate here.
>
> Execution will apply the approved `change-plan.json`, run the bounded pre-validator checks, and create one commit if those checks pass. I’m delegating that implementation stage now.
>
> Execution is now in progress. This is the long stage: dependency edits, pre-validator setup/startup/CLI checks, repairs if clearly plan-related, and then a single commit if everything passes.
</details>

**Stage 4 Failed**

Execution halted before applying upgrade changes.

Reason:
`git status` showed pre-existing non-test changes that are not part of `validation.generated_test_files`:

- `.claude/settings.local.json` deleted
- `AGENTS.md` modified
- `CLAUDE.md` modified
- `tests/` generated by Stage 3

Per the upgrade pipeline, the executor cannot proceed or commit while unrelated pre-existing changes are present. No planned upgrade changes were applied and no commit was created.

Execution artifact:
`C:\Users\OEM\legacy-upgrade-system\.codex\upgrade-runs\tv-radio\20260805-153534-startup\execution-result.json`

To continue, the unrelated repo changes need to be committed, stashed, or otherwise resolved by you, then I can resume execution on the same branch/artifacts.

> commit and continue

<details><summary>6 previous messages</summary>

> I’ll commit only the pre-existing non-upgrade files that blocked the executor, leaving the generated Stage 3 tests uncommitted so they can still be included in the final upgrade commit. Then I’ll rerun Stage 4 against the same branch and artifacts.
>
> The blocking changes are exactly the three pre-existing files plus the generated `tests/` directory. I’m committing the three pre-existing files only.
>
> The blocker commit is `67ea3f7`. I’m checking that only the generated tests remain dirty, then I’ll rerun the execution sub-agent.
>
> The worktree now has only the Stage 3 generated tests pending, which the executor is allowed to carry into the upgrade commit. I’m rerunning Stage 4 with the same approved plan.
>
> Execution has restarted. If dependency installation or startup checks hit environment issues, the executor should record them in the execution result; I’ll report the exact blocking command if it fails.
>
> Stage 4 is still running after ten minutes. That likely means dependency setup or startup checks are taking time; I’m continuing to wait rather than interrupting the executor mid-validation.
</details>

Stage 4 stopped before commit.

What happened:
- The executor applied some planned changes and used all `3` allowed scope expansions.
- Frontend startup then found another out-of-plan Express 5 wildcard route:
  - `tv-radio-frontend/content/public-viewer/client-public-viewer.js`
  - route: `/public/viewer/*`
- Because the approved plan allowed only 3 scope expansions, the executor had to halt instead of patching that fourth out-of-plan file.

No upgrade commit was created.

Execution artifact:
`C:\Users\OEM\legacy-upgrade-system\.codex\upgrade-runs\tv-radio\20260805-153534-startup\execution-result.json`

To continue, I need your approval to expand the plan for this additional Express 5 route compatibility fix, then rerun execution.

> expand the plan and retry

<details><summary>15 previous messages</summary>

> I’ll treat this as an approved plan revision for the newly discovered Express 5 route file, then rerun execution. First I’m reading the execution result and current status so the planner update reflects the exact halted state.
>
> The failed executor left applied upgrade edits in the worktree, so this retry is a continuation rather than a clean fresh Stage 4. I’m delegating a narrow plan revision: add the discovered `client-public-viewer.js` Express 5 route fix and record that the already-applied scope expansions are now approved plan entries for the retry.
>
> The plan revision is running. Once it returns, I’ll verify the updated `change-plan.json` and restart execution as a continuation of the halted attempt.
>
> The plan is expanded to 53 changes and preserves all validation commands. I’m confirming the new route file is planned, then I’ll rerun the executor in continuation mode.
>
> The revised plan passes the gate check: `client-public-viewer.js` is now planned, and the continuation has two remaining bounded scope expansions. I’m restarting Stage 4 with explicit context that the dirty worktree consists of the previous failed execution’s in-progress planned changes.
>
> The retry hit the sub-agent concurrency limit because completed stage agents were still open. I’m closing the completed agents, then I’ll launch the continuation executor again.
>
> The completed stage agents are closed. I’m launching the continuation executor now with the expanded plan.
>
> The continuation executor is running. I’ll wait through the full pre-validator sequence since it must repeat setup, startup probes, CLI commands, and generated tests before it can commit.
>
> The continuation is still running after ten minutes. It is past the earlier failure point, so I’m giving it more time to finish the remaining smoke commands and commit.
>
> Stage 4 is still not done after the additional wait. I’m checking repo status and the execution artifact timestamp from the orchestration side without disturbing the running executor.
>
> The worktree shows the new public-viewer route and a couple of additional direct dependency files being patched, which matches the retry path. The execution artifact has not updated yet, so the sub-agent is still working through checks or repairs.
>
> The executor is still running after an extended wait. I’m going to inspect active Node processes and recent startup logs so I can tell whether it is genuinely progressing or stuck on a long-running command.
>
> There are several Node processes from startup checks still alive, and the latest error logs are environment/config warnings rather than a clear dependency failure. I’m giving the executor one more bounded wait for a final handoff before deciding whether it’s stuck.
>
> The executor has not returned after another wait, so I’m checking whether it managed to commit or is still leaving the worktree uncommitted. If it is stuck, I’ll report the current blocking state instead of pretending Stage 4 finished.
>
> No upgrade commit exists yet, and `execution-result.json` has not changed since the prior failure. I’m asking the executor for an immediate status/update; if it does not respond, I’ll close it and rerun the remaining Stage 4 work locally or with a fresh narrowed executor.
</details>

Stage 4 failed again before commit.

Current blocker:
- `node chapman.js index --verbose` from `tv-radio-admin/CLI`
- It failed twice with async double-callback / `pace` zero-total signatures, then the final rerun hung and was interrupted by the status check.
- No upgrade commit was created.

What did pass before the blocker:
- 8 `npm install` setup checks
- 2 `node --check` compile checks
- Admin/API startup at `http://localhost:8080/`
- Frontend startup after the expanded Express 5 route repair
- `node chapman.js resetindex --verbose`

Current state:
- Upgrade changes are still uncommitted in the worktree.
- Execution artifact: `C:\Users\OEM\legacy-upgrade-system\.codex\upgrade-runs\tv-radio\20260805-153534-startup\execution-result.json`
- Branch: `upgrade/dependency-modernization-solr`
- Separate pre-existing docs/settings commit remains: `67ea3f7`

Per the pipeline, execution is halted because the required CLI smoke command is failing/hanging. To proceed, the plan needs another approved expansion covering the `chapman.js index` failure path.

> expand to cover the chapman failure path

<details><summary>11 previous messages</summary>

> I’ll expand the approved plan around the `chapman.js index` failure path, specifically the async double-callback and `pace` zero-total behavior surfaced by the required CLI smoke command. First I’m closing the last executor agent and gathering the exact CLI failure context for the planning revision.
>
> The artifact confirms the failing path is within `tv-radio-admin/Search/build.js` and `tv-radio-admin/CLI/chapman.js`, both tied to the approved async/pace dependency upgrade surface and empty datastore validation. I’m updating the plan narrowly for those files, preserving all existing validation checks.
>
> The narrow planning revision is running now. After it returns, I’ll verify the updated plan includes `chapman.js` and `Search/build.js`, then retry Stage 4 continuation.
>
> The plan revision is taking longer than expected, likely because it is reconciling the existing in-progress changes with the current plan. I’m waiting a bit longer rather than editing the plan in parallel.
>
> The plan now includes the `chapman.js` index failure path and has 54 planned changes. I’m checking the gate fields, then I’ll retry Stage 4 continuation against the updated plan.
>
> The revised plan includes the CLI and Search repair files. I’m retrying execution again, explicitly treating current dirty files as the in-progress planned work from the previous failed attempts.
>
> The executor is running again. This pass should get through the previously blocked `chapman.js index` path or return a specific failure if the CLI command still hangs after the approved repairs.
>
> Stage 4 has passed and created the upgrade commit `ab76946`. I’m moving to Stage 5 final validation now, which re-runs the declared checks and can amend that commit if it finds plan-related issues.
>
> The validator instructions are loaded. I’m spawning the final validation agent with the single permitted artifact input, `change-plan.json`, plus the branch and repo details.
>
> Final validation is running. This includes git diff alignment, content checks, build/startup checks, CLI smoke commands, generated tests, and browser console inspection for the two health URLs.
>
> Stage 5 finished with a rejection after amending the upgrade commit to `44970c6`. I’m reading the validation report now so I can report the exact final decision, changed files, repairs, and failures from the authoritative JSON.
</details>

**Stage 5 Rejected**

Final validation report:
`C:\Users\OEM\legacy-upgrade-system\.codex\upgrade-runs\tv-radio\20260805-153534-startup\validation-report.json`

Branch:
`upgrade/dependency-modernization-solr`

Final upgrade commit after validation repair:
`44970c6`

What passed:
- Build checks passed.
- Admin/API startup wrapper exited `0` and returned HTTP `200`.
- Frontend startup wrapper exited `0` and returned HTTP `200`.
- CLI smoke commands passed by exit status:
  - `resetindex`
  - `index`
  - `geodata`
  - `courses`
- Generated tests passed.
- Content checks passed.

Repair applied:
- `tv-radio-frontend/app.js`: fixed upgraded `uglify-js` handling so `/libs/client.js` serves real bundled code instead of filename tokens. The upgrade commit was amended.

Why validation still rejected:
- Playwright captured `/favicon.ico` 404 console errors on both health URLs:
  - `http://localhost:8080/`
  - `http://localhost:8082/`
- The validator classified those favicon errors as out of scope, but the strict startup rule still rejects any browser console error.
- Diff also includes unexpected files relative to the plan, including the earlier local agent docs/settings commit and generated lockfiles/vendored assets not explicitly listed in the ChangePlan.

Stage 6 is skipped because Stage 5 was rejected.

Token usage: total=216,823 input=189,838 (+ 2,817,664 cached) output=26,985 (reasoning 1,797)
Token usage: total=142,975 input=133,191 (+ 1,880,448 cached) output=9,784 (reasoning 1,962)

errors:
  One remaining environment issue: I queried local Solr directly and it returns:

  undefined field: "genres_sM"

  The repo schema at tv-radio-admin/Settings/Solr/schema.xml defines *_sM, but the running tvradio Solr core has the default Solr schema instead. Rebuild/recreate the Solr
  core/configset from tv-radio-admin/Settings/Solr, then run the index build, for example node chapman.js resetindex --verbose / node chapman.js index --verbose from tv-radio-
  admin/CLI once the correct schema is loaded.

  TypeError: request.query.hasOwnProperty is not a function
    at __commonSearchOptionsForRequest (C:\Users\OEM\legacy-upgrade-system\projects\tv-radio\tv-radio-admin\Admin\API\admin-live-titles.js:613:47)
    at _performGeneralLiveTitleSearch (C:\Users\OEM\legacy-upgrade-system\projects\tv-radio\tv-radio-admin\Admin\API\admin-live-titles.js:644:23)
    at C:\Users\OEM\legacy-upgrade-system\projects\tv-radio\tv-radio-admin\Admin\API\admin-live-titles.js:132:9
    at Layer.handleRequest (C:\Users\OEM\legacy-upgrade-system\projects\tv-radio\tv-radio-admin\Admin\API\node_modules\router\lib\layer.js:152:17)
    at next (C:\Users\OEM\legacy-upgrade-system\projects\tv-radio\tv-radio-admin\Admin\API\node_modules\router\lib\route.js:157:13)
    at Route.dispatch (C:\Users\OEM\legacy-upgrade-system\projects\tv-radio\tv-radio-admin\Admin\API\node_modules\router\lib\route.js:117:3)
    at handle (C:\Users\OEM\legacy-upgrade-system\projects\tv-radio\tv-radio-admin\Admin\API\node_modules\router\index.js:435:11)
    at Layer.handleRequest (C:\Users\OEM\legacy-upgrade-system\projects\tv-radio\tv-radio-admin\Admin\API\node_modules\router\lib\layer.js:152:17)
    at C:\Users\OEM\legacy-upgrade-system\projects\tv-radio\tv-radio-admin\Admin\API\node_modules\router\index.js:295:15
    at processParams (C:\Users\OEM\legacy-upgrade-system\projects\tv-radio\tv-radio-admin\Admin\API\node_modules\router\index.js:582:12)

    node app.js
{"level":"info","message":"Listening on port %d."}
{"level":"error","message":"[Data/Homepage] generating list of most populous genres failed due to: Error: Solr response didn't contain a valid status code in response header (400). undefined field: \"genres_sM\""}
{"error":"TypeError: Cannot read properties of undefined (reading 'toString')","errorCode":"D92738CA-71E1-4DA1-B157-F31E9DA7C538","level":"error","message":"[Content/TVRadio] issue during page rendering occurred","source":"template-render"}

ember-template-compiler-v1.13.13.js:282 DEPRECATION: The `bind-attr` helper (L1:C17) is deprecated in favor of HTMLBars-style bound attributes.
        at isBindAttrModifier (ember-template-compiler-v1.13.13.js:11519:34)
        at ember-template-compiler-v1.13.13.js:11412:15
        at Walker.visit (ember-template-compiler-v1.13.13.js:17975:7)
        at Program (ember-template-compiler-v1.13.13.js:17985:16)
        at Walker.children (ember-template-compiler-v1.13.13.js:18008:7)
        at Walker.visit (ember-template-compiler-v1.13.13.js:17976:12)
        at TransformBindAttrToAttributes.TransformBindAttrToAttributes_transform [as transform] (ember-template-compiler-v1.13.13.js:11407:12)
        at Object.preprocess (ember-template-compiler-v1.13.13.js:17374:27)
        at compileSpec (ember-template-compiler-v1.13.13.js:12529:37)
(anonymous) @ ember-template-compiler-v1.13.13.js:282
ember-template-compiler-v1.13.13.js:282 DEPRECATION: The `bind-attr` helper (L1:C142) is deprecated in favor of HTMLBars-style bound attributes.
        at isBindAttrModifier (ember-template-compiler-v1.13.13.js:11519:34)
        at ember-template-compiler-v1.13.13.js:11412:15
        at Walker.visit (ember-template-compiler-v1.13.13.js:17975:7)
        at Program (ember-template-compiler-v1.13.13.js:17985:16)
        at Walker.children (ember-template-compiler-v1.13.13.js:18008:7)
        at Walker.visit (ember-template-compiler-v1.13.13.js:17976:12)
        at TransformBindAttrToAttributes.TransformBindAttrToAttributes_transform [as transform] (ember-template-compiler-v1.13.13.js:11407:12)
        at Object.preprocess (ember-template-compiler-v1.13.13.js:17374:27)
        at compileSpec (ember-template-compiler-v1.13.13.js:12529:37)
(anonymous) @ ember-template-compiler-v1.13.13.js:282
ember-template-compiler-v1.13.13.js:282 DEPRECATION: The `bind-attr` helper (L1:C101) is deprecated in favor of HTMLBars-style bound attributes.
        at isBindAttrModifier (ember-template-compiler-v1.13.13.js:11519:34)
        at ember-template-compiler-v1.13.13.js:11412:15
        at Walker.visit (ember-template-compiler-v1.13.13.js:17975:7)
        at ElementNode (ember-template-compiler-v1.13.13.js:17991:16)
        at Walker.children (ember-template-compiler-v1.13.13.js:18008:7)
        at Walker.visit (ember-template-compiler-v1.13.13.js:17976:12)
        at Program (ember-template-compiler-v1.13.13.js:17985:16)
        at Walker.children (ember-template-compiler-v1.13.13.js:18008:7)
        at Walker.visit (ember-template-compiler-v1.13.13.js:17976:12)
(anonymous) @ ember-template-compiler-v1.13.13.js:282
ember-template-compiler-v1.13.13.js:282 DEPRECATION: The `bind-attr` helper (L1:C44) is deprecated in favor of HTMLBars-style bound attributes.
        at isBindAttrModifier (ember-template-compiler-v1.13.13.js:11519:34)
        at ember-template-compiler-v1.13.13.js:11412:15
        at Walker.visit (ember-template-compiler-v1.13.13.js:17975:7)
        at ElementNode (ember-template-compiler-v1.13.13.js:17991:16)
        at Walker.children (ember-template-compiler-v1.13.13.js:18008:7)
        at Walker.visit (ember-template-compiler-v1.13.13.js:17976:12)
        at Program (ember-template-compiler-v1.13.13.js:17985:16)
        at Walker.children (ember-template-compiler-v1.13.13.js:18008:7)
        at Walker.visit (ember-template-compiler-v1.13.13.js:17976:12)
(anonymous) @ ember-template-compiler-v1.13.13.js:282
:8080/api//media-scan:1  Failed to load resource: the server responded with a status of 404 (Not Found)
ember-template-compiler-v1.13.13.js:282 DEPRECATION: Using `{{controller}}` or any path based on it ('application' @ L2:C24) has been deprecated. [deprecation id: view.keyword.controller] See http://emberjs.com/deprecations/v1.x#toc_view-and-controller-template-keywords for more details.
        at deprecatePath (ember-template-compiler-v1.13.13.js:11267:32)
        at deprecatePaths (ember-template-compiler-v1.13.13.js:11262:7)
        at deprecateHash (ember-template-compiler-v1.13.13.js:11251:7)
        at ember-template-compiler-v1.13.13.js:11237:7
        at Walker.visit (ember-template-compiler-v1.13.13.js:17975:7)
        at Program (ember-template-compiler-v1.13.13.js:17985:16)
        at Walker.children (ember-template-compiler-v1.13.13.js:18008:7)
        at Walker.visit (ember-template-compiler-v1.13.13.js:17976:12)
        at DeprecateViewAndControllerPaths.DeprecateViewAndControllerPaths_transform [as transform] (ember-template-compiler-v1.13.13.js:11230:12)
(anonymous) @ ember-template-compiler-v1.13.13.js:282
ember-template-compiler-v1.13.13.js:282 DEPRECATION: Using `{{view}}` or any path based on it ('application' @ L6:C3) has been deprecated. [deprecation id: view.keyword.view] See http://emberjs.com/deprecations/v1.x#toc_view-and-controller-template-keywords for more details.
        at deprecatePath (ember-template-compiler-v1.13.13.js:11267:32)
        at ember-template-compiler-v1.13.13.js:11235:7
        at Walker.visit (ember-template-compiler-v1.13.13.js:17975:7)
        at ElementNode (ember-template-compiler-v1.13.13.js:17991:16)
        at Walker.children (ember-template-compiler-v1.13.13.js:18008:7)
        at Walker.visit (ember-template-compiler-v1.13.13.js:17976:12)
        at ElementNode (ember-template-compiler-v1.13.13.js:17991:16)
        at Walker.children (ember-template-compiler-v1.13.13.js:18008:7)
        at Walker.visit (ember-template-compiler-v1.13.13.js:17976:12)
(anonymous) @ ember-template-compiler-v1.13.13.js:282
:8080/api//unisat/scan:1  Failed to load resource: the server responded with a status of 404 (Not Found)
:8080/api//usage/state:1  Failed to load resource: the server responded with a status of 404 (Not Found)
notifications.js:89 auth result data:
notifications.js:90 Object
notifications.js:98 SocketIO authorisation failed, unable to obtain JSONWebToken from API service.
>node chapman.js geodata --verbose
[INFO/INGEST-GEO-DATA] Reading in admin codes from path: C:\Users\OEM\legacy-upgrade-system\projects\tv-radio\tv-radio-admin\Data\admin1CodesASCII.txt
[INFO/INGEST-GEO-DATA] Completed reading in admin codes.
[INFO/INGEST-GEO-DATA] Starting import of 'country' data from path: C:\Users\OEM\legacy-upgrade-system\projects\tv-radio\tv-radio-admin\Data\countryInfo.txt
[ERR/INGEST-GEO-DATA] unable to add country data to search index due to error -> Error: invalid response status code (400)