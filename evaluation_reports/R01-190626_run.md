# Evaluation R1: `190626_run.md` (tv-radio, 19 Jun 2026)

Verdict: the pipeline approved this run at confidence 0.82, but the upgraded frontend cannot load. FBSR = 0.

| Item | Value |
| --- | --- |
| Chat | `refactored_chats/190626_run.md` |
| Target | tv-radio, all 8 npm packages |
| Upgrade request | "upgrade any outdated dependencies found" |
| Validation commands from user | none (no build or start command given) |
| Branch and commit evaluated | `upgrade/upgrade-any-outdated-dependencies-found` at `82dd113` (preflight commit `6847e53`) |
| Run artifacts | `.codex/upgrade-runs/tv-radio/20260619-231714-dependency-upgrade/` |
| Tool version | `88724bc` / `047c87f` (first Codex-based version) |
| Pipeline verdict | approved, confidence 0.82, 0 repairs |
| Human decisions beyond the two gates | 1 (commit the dirty working tree) |

## 1.1 Build effectiveness

| Metric | Value | Evidence |
| --- | --- | --- |
| C (install and boot) | 0 | Booted today after a fresh `npm install`: the frontend exits on load with `TypeError: influx is not a function` at `metrics.js:39`. influx was bumped 4.0.0 to 5.12.0 but `metrics.js` was not in the plan. Six routes still use bare `*` patterns (`client-public-viewer.js`, `client-tv-radio.js` x2, `client-unisat.js` x2, `usage-monitoring.js`), which Express 5.2.1 rejects at registration. The Admin API boots and serves HTTP 200 on `/`. |
| T (declared checks) | 0 | No checks were declared; the validator ran `npm install` in the 8 packages and one generated smoke test. An application that cannot start fails any oracle. |
| T (extended) | 0 | As above, plus `request.query.hasOwnProperty` in 5 places of the Admin API under Express 5, geodata ingest still assigns `q.drain`, `uglify.minify` still receives a filename array. |
| FBSR contribution | 0 | |
| Dependency coverage | 49 of 51 entries changed | Only `pace@0.0.4` and `optimist@0.6.1` unchanged; both have no newer release. |
| Syntax | 17 changed `.js` files, 0 `node --check` failures | |

What the pipeline itself checked: 7 content checks (all passed), an install loop over the 8 packages (passed), `node --test tests/dependency-upgrade-smoke.test.js` (passed). No process was started. The validation report lists 43 planned files that were never touched and 9 unexpected files (the agent instruction files), and still approved.

## 1.2 Generated tests

| Test | What it asserts | Result | Classification |
| --- | --- | --- | --- |
| `tests/dependency-upgrade-smoke.test.js` (`node --test`) | manifest version strings and that upgraded modules can be required | passed | false negative: the frontend cannot load and six routes are invalid |

TP 0, FP 0, FN 1. Recall 0.

## 1.3 Behaviour preservation

| Behaviour | Status | Evidence |
| --- | --- | --- |
| b1 Frontend loads all modules | missing | boot today: `influx is not a function` |
| b2 Frontend registers routes and serves `/` | missing | six bare `*` routes under Express 5 (deterministic registration error) |
| b3 Frontend content modules resolve dependencies | missing | frontend cannot start |
| b4 Bare `/search/` route matches | missing | route is `/search/*` and the process cannot start |
| b5 Search page renders | not observed | |
| b6 Client bundle `/libs/client.js` valid | missing | `uglify.minify(concatJS, ...)` with a filename array under uglify-js 3.19 |
| b7 Admin title search handles query options | missing | 5 `request.query.hasOwnProperty` calls with Express 5.2.1 |
| b8 Admin API boots and serves `/` | preserved | boot today: HTTP 200 |
| b9 `chapman.js index` completes | missing (inferred) | `Search/build.js` and `chapman.js` unchanged from the code that failed in R4 with the same dependency set |
| b10 `chapman.js geodata` completes | missing | three `q.drain = function` assignments in `city-country-dataset.js` with async 3.2.6 |
| b11 Frontend survives an error log | not observed | process fails earlier |
| b12, b13 | not observed | |

Observed 9, preserved 1: BPR 0.11, MBR 0.89. New behaviours: none recorded (NBR 0). EBER 0.

## 1.4 Cost and efficiency

All threads of the run, from the Codex session logs (UTC 19 Jun 11:16 to 12:05):

| Thread | Effort | Minutes | Input tokens | Note |
| --- | --- | --- | --- | --- |
| Orchestrator (main) | medium | 56 | 1,219,663 | 15 user turns |
| Repository analysis | medium | 10 | 1,102,951 | |
| Planning | medium | 6 | 659,805 | |
| Test generation | medium | 2 | 341,324 | |
| Execution | medium | 8 | 3,635,120 | |
| Final validation | medium | 17 | 484,482 | |
| Total | | 56 wall | 7,443,345 (6,725,248 cached, 718,097 uncached) | output 89,052 (reasoning 12,968) |

Notional cost at GPT-5.5 list prices: US$9.62. The chat's own token line (main thread only) reads total 169,717, input 155,892 (+945,280 cached), output 13,825, which the user priced at about US$1.67; the sub-agents multiply that by roughly six.

Stage timings the user recorded: preflight 28 s, analysis 10 m 37 s, planning 7 m 16 s, test generation 3 m 6 s, execution and validation 26 m 49 s; about 48 minutes of agent activity. Context window used: 49%.

## 1.5 LLM configuration

GPT-5.5 via Codex CLI, reasoning effort medium on every thread, Windows 11, Node v24.15.0, GitNexus indexed. The analyzer reported that GitNexus gave file-level context and that line ranges came from targeted searches.

## Stage outcomes

| Stage | Result |
| --- | --- |
| Analysis | 67 affected files, 35 high risk, 10 breaking-change families; vendored browser libraries excluded |
| Plan | 28 ordered changes over 67 planned files, 51 expected dependency changes, 0 executor check commands, 7 content checks |
| Execution | passed, commit `82dd113`, 24 files changed, 8 changes applied, 4 skipped as "no edit required" |
| Validation | approved 0.82; 34 files in diff, 9 unexpected, 43 planned files untouched, 51 dependency changes verified, 0 repairs |

## What was broken afterwards

The frontend does not start (influx 5, Express 5 routes). The Admin API starts but its title-search endpoints throw on any query option. The geodata ingest and index build are broken by the async 3 and pace behaviour that R4 later hit. None of this was visible to the user at the time because no runtime check was requested or inferred.

## Run-specific recommendations

- With no user command, the planner should infer `node app.js` for each `app.js` it finds and record bounded startup checks; approving with 0 executor checks should be impossible.
- Files that call an upgraded package but are absent from the plan (`metrics.js` calling influx) should be flagged by the analyzer as high risk rather than left to runtime.
- The 43 untouched "planned files" should count against the confidence score; here they did not.
