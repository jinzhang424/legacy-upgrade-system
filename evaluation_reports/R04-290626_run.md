# Evaluation R4: `290626_run.md` (tv-radio, 29 Jun 2026)

Verdict: both health URLs answered and the run was approved at 0.86 with no repairs, but `chapman.js index` failed as soon as the user tried it, the Admin search endpoint throws, and the client bundle and geodata ingest are broken. This was also the most expensive run before August (US$19.45) and the only one whose sub-agents ran at high reasoning effort. FBSR = 1 on the declared checks, 0 extended.

| Item | Value |
| --- | --- |
| Chat | `refactored_chats/290626_run.md` |
| Target | tv-radio, all packages |
| Upgrade request | "upgrade any outdated dependencies found in the tv-radio repo" |
| Validation commands from user | Admin API: `node app.js` and verify `localhost:8080`; frontend: `node app.js` and verify `localhost:8082` |
| Branch and commit evaluated | `upgrade/upgrade-outdated-deps-20260629` at `5c55798` (metadata commit `60a51c6`) |
| Run artifacts | `.codex/upgrade-runs/tv-radio/20260629-160159-startup/` (an aborted start left the empty `20260629-154316-startup/`) |
| Tool version | `36cbfad` to `4a9a3b6` |
| Pipeline verdict | approved, confidence 0.86, 0 repairs |
| Human decisions beyond the two gates | 1 (commit dirty tree) |

## 1.1 Build effectiveness

| Metric | Value | Evidence |
| --- | --- | --- |
| C (install and boot) | 1 | Booted today: frontend loads, Admin API serves HTTP 200. Pipeline startups passed after 3 (Admin) and 1 (frontend) repair rounds. |
| T (declared checks) | 1 | Validator: syntax checks, Admin health at `http://localhost:8080/`, frontend health at `http://localhost:8082/`, both generated tests, content checks all passed. User: "the frontend works and the admin code compiled with no errors". |
| T (extended) | 0 | User note at the end of the chat: "failed with chapman.js indexing". Static: 5 `hasOwnProperty` calls with Express 5; `uglify.minify(concatJS, {` filename array; async 3 `q.drain =` in the ingester. |
| Dependency coverage | 49 of 51 | |
| Syntax | 24 changed `.js` files, 0 failures | |

The Admin health check passed partly because the executor added a new root route: "Added an API-only root health response without shadowing full web interface mode" (an unrequested behaviour, counted under NBR).

## 1.2 Generated tests

| Test | What it asserts | Result | Classification |
| --- | --- | --- | --- |
| `upgrade-tests/frontend-request-common-smoke.test.js` | `request-common.js` loads and its helpers behave | passed | false negative |
| `upgrade-tests/common-mongo-backup-integration.test.js` | the Mongo backup count helper against a stub | passed after 1 executor repair | false negative |

TP 0, FP 0, FN 1.

## 1.3 Behaviour preservation

| Behaviour | Status | Evidence |
| --- | --- | --- |
| b1 Frontend loads all modules | preserved | boot today |
| b2 Frontend registers routes and serves `/` | preserved | validator health check; user |
| b3 Content modules resolve dependencies | preserved | `uuid` 14.0.1 kept |
| b4 Bare `/search/` route matches | preserved | regex routes `/^\/search\/.*$/` |
| b5 Search page renders | not observed | |
| b6 Client bundle valid | missing | `uglify.minify(concatJS, {` with uglify-js 3 |
| b7 Admin title search | missing | 5 `hasOwnProperty` calls |
| b8 Admin API boots and serves `/` | preserved | boot today; validator |
| b9 `chapman.js index` completes | missing | user: "failed with chapman.js indexing" |
| b10 `chapman.js geodata` completes | missing | async 3 `q.drain =` |
| b11 Frontend survives an error log | missing | boot today: `common.log is not a function` in winston-logstash |
| b12, b13 | not observed | |

Observed 10, preserved 5: BPR 0.50, MBR 0.50. New behaviour: API-only root health response (1), NBR 1 / 6 = 0.17 for this run.

## 1.4 Cost and efficiency

| Thread | Effort | Minutes | Input tokens | Note |
| --- | --- | --- | --- | --- |
| Aborted orchestrator starts (2) | medium | 1 + 6 | 101,097 + 64,515 | left the empty artifact folder |
| Orchestrator (main) | medium | 90 | 2,137,071 | 14 user turns |
| Repository analysis | medium | 6 | 609,895 | |
| Planning | high | 7 | 1,186,034 | |
| Test generation | medium | 2 | 461,755 | |
| Execution (halted on dirty tree) | high | 1 | 86,158 | |
| Execution rerun | high | 27 | 13,056,470 | the largest single thread in all ten runs |
| Final validation | high | 9 | 1,152,644 | |
| Total | | 90 wall (109 including aborted starts) | 18,855,639 (17,511,424 cached, 1,344,215 uncached) | output 132,335 (reasoning 31,345) |

Notional cost: US$19.45. The chat has no token counter (the session did not expose one; the model estimated US$0.90 to US$1.40 for the visible transcript and guessed "a few dollars" including sub-agents; the real all-thread figure is about 14 times its low estimate). Stage timings the user recorded: 55 s, 6 m 53 s, 8 m, 5 m 6 s, 39 m 4 s; about 60 minutes. Context used: 94%.

## 1.5 LLM configuration

GPT-5.5 via Codex CLI. The session logs show `high` reasoning effort on the planning, execution and validation threads and `medium` on the orchestrator, analysis and test threads; the skill files contain no effort setting, so the cause is outside the repository (a Codex-side setting at the time). Windows 11, Node v24.15.0, GitNexus indexed.

## Stage outcomes

| Stage | Result |
| --- | --- |
| Analysis | 73 affected files, 67 high risk |
| Plan | 81 ordered changes over 81 files, 18 executor checks, 2 startup checks with health URLs, lockfile generation for 8 roots |
| Execution | halted once on the dirty tree; rerun passed, commit `5c55798`, 39 files, 5 repairs (MongoDB 7 connect and countDocuments handling, API root route, `/media/access/*` wrapper in `app.js`, sibling package installs), 20 checks passed |
| Validation | approved 0.86; 48 files in diff, 9 unexpected (agent files), 44 planned files untouched, 7 dependency changes verified, 9 commands passed, 0 repairs |

## What was broken afterwards

`chapman.js index` (empty index plus pace progress bar and async double callback, fixed by the executor in R5), Admin title search, client bundle, geodata ingest, logger error path.

## Run-specific recommendations

- A health check that passes because the executor added a new root route is not evidence that the existing application works; new routes added to satisfy a check should be flagged.
- The 13.1 M-token execution rerun re-read the whole plan and repository after a dirty-tree halt; state should carry over between attempts.
- The chat's cost estimate was off by an order of magnitude; the final handoff should print all-thread usage.
