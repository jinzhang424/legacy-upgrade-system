# Evaluation R2: `230626_run.md` (tv-radio frontend only, 23 Jun 2026)

Verdict: approved at 0.86 after the validator repaired two route patterns; the frontend starts, but `uuid` was removed while two content modules still require it, and the client bundle and search route are broken. FBSR = 0 (1 on the declared check alone).

| Item | Value |
| --- | --- |
| Chat | `refactored_chats/230626_run.md` |
| Target | tv-radio, `tv-radio-frontend` only; `tv-radio-admin` excluded by the user |
| Upgrade request | "upgrade any outdated dependencies in the repo"; compile by `node app.js` in the frontend; do not run admin code |
| Validation commands from user | `node app.js` in `tv-radio-frontend` |
| Branch and commit evaluated | `upgrade/upgrade-outdated-dependencies-20260623` at `3874441` (execution commit `a02abaf`, amended by the validator; baseline commit `e7c5c97`) |
| Run artifacts | `.codex/upgrade-runs/tv-radio/20260623-103608-startup/` |
| Tool version | `36cbfad` |
| Pipeline verdict | approved, confidence 0.86, 2 validator repairs |
| Human decisions beyond the two gates | 3 (commit dirty tree; approve scope expansion for `client-public-viewer.js`; approve expansion for `usage-monitoring.js`) |

## 1.1 Build effectiveness

| Metric | Value | Evidence |
| --- | --- | --- |
| C (install and boot) | 1 | Booted today: the frontend loads its modules and blocks waiting for MongoDB (not running); the pipeline's bounded `node app.js` passed after five executor repairs. Admin packages are untouched and identical to the original. |
| T (declared check) | 1 | `node app.js` startup check passed for the executor and the validator; `node --check app.js` passed; both generated tests passed. |
| T (extended) | 0 | The user's own follow-up: `uuid` was removed from `package.json` but `client-live-streams.js` and `client-primo-viewer.js` still `require("uuid")` and call `uuid.v4()`; the client bundle is built with a filename array under uglify-js 3; `/search/*path` no longer matches the bare `/search/` page. |
| Dependency coverage | 21 of 51 entries (all in the frontend); `uuid` and `gc-stats` removed rather than bumped | 30 admin entries untouched by user choice |
| Syntax | 20 changed `.js` files, 0 `node --check` failures | |

## 1.2 Generated tests

| Test | What it asserts | Result | Classification |
| --- | --- | --- | --- |
| `tv-radio-frontend/test/upgrade-smoke.test.js` | module loading and manifest state | passed | false negative |
| `tv-radio-frontend/test/upgrade-integration.test.js` | migrated call sites with mocks | passed | false negative |

TP 0, FP 0, FN 1 (run level). The user's note on this run: "Testing agent was poor, would probs need to increase the amount of testing done."

## 1.3 Behaviour preservation

Admin-side behaviours are preserved by construction: the admin packages were not changed.

| Behaviour | Status | Evidence |
| --- | --- | --- |
| b1 Frontend loads all modules | preserved | boot today |
| b2 Frontend registers routes and serves `/` | preserved | pipeline startup checks after repairs |
| b3 Content modules resolve dependencies | missing | `uuid` removed; 2 modules still require it |
| b4 Bare `/search/` route matches | missing | route is `/search/*path`; a named wildcard needs at least one segment |
| b5 Search page renders | not observed | |
| b6 Client bundle valid | missing | `uglify.minify(concatJS, ...)` unchanged with uglify-js 3.19.3 |
| b7 Admin title search | preserved | admin untouched (Express 4.9.5) |
| b8 Admin API boots | preserved | boot today |
| b9 `chapman.js index` | preserved | admin untouched |
| b10 `chapman.js geodata` | preserved | admin untouched |
| b11 Frontend survives an error log | preserved | boot today: the MongoDB failure was logged and the process stayed alive |
| b12 Admin UI API URLs | preserved | admin untouched |
| b13 Socket.IO client auth | preserved | admin untouched |

Observed 12, preserved 9: BPR 0.75, MBR 0.25. New behaviours: none. Scoped to the frontend alone, 3 of 6 observed frontend behaviours were lost.

## 1.4 Cost and efficiency

| Thread | Effort | Minutes | Input tokens | Note |
| --- | --- | --- | --- | --- |
| Orchestrator (main) | medium | 98 | 2,147,538 | 16 user turns, includes waiting at gates |
| Repository analysis | medium | 5 | 959,634 | |
| Planning | medium | 5 | 527,296 | |
| Test generation | medium | 2 | 457,918 | |
| Execution (resumed twice for scope expansions) | medium | 15 | 5,312,128 | 5 repair rounds |
| Final validation | medium | 4 | 671,272 | 1 repair round, 2 route fixes |
| Total | | 98 wall | 10,075,786 (9,074,560 cached, 1,001,226 uncached) | output 87,090 (reasoning 13,479) |

Notional cost: US$12.16 (the chat's own estimate was US$2.54 for the main thread). Stage timings the user recorded: preflight 28 s, analysis 6 m 15 s, planning 6 m 17 s, test generation 2 m 52 s, execution and validation 8 m 3 s + 3 m 30 s + 8 m 47 s; about 36 minutes of agent activity against 98 minutes of wall time. Context used: 46%.

## 1.5 LLM configuration

GPT-5.5 via Codex CLI, medium effort on all six threads, Windows 11, Node v24.15.0, GitNexus indexed (line ranges still came from `rg`).

## Stage outcomes

| Stage | Result |
| --- | --- |
| Analysis | 21 affected files, 9 high risk, admin excluded |
| Plan | 23 ordered changes (21 plus 2 approved expansions), 21 expected dependency changes, 3 executor checks (install, `node --check`, bounded startup), 7 content checks |
| Execution | passed, commit `a02abaf`, 22 files; 5 repairs, all Express 5 wildcard or Influx 5 shape fixes discovered at startup |
| Validation | approved 0.86, amended to `3874441`; 31 files in diff, 9 unexpected (agent files), 22 dependency changes verified; repaired `/public/viewer/*path` and `/media/access/*path` to the `{*path}` form |

## What was broken afterwards

The `uuid` removal is a genuine defect (the user's own dependency tally calls it "a genuine failure, not just no upgrade needed"). The client bundle and bare search route were not exercised by anyone until R7. Execution required two human approvals to touch files the analysis had missed.

## Run-specific recommendations

- Removing a dependency must be paired with a repo-wide `require` scan; the analyzer had the tool for it and did not run it.
- Two of the five executor repairs were scope expansions that needed a human each; this run is the origin of the later `scope_expansion_limit`.
- The wall time is 2.7 times the agent time; approval gates are where the minutes go.
